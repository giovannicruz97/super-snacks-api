const machine = require('./MachineValidator');
const product = require('./ProductValidator');
const card = require('./CardValidator');
const sale = require('./SaleValidator');

module.exports = {
  machine,
  product,
  card,
  sale
};
